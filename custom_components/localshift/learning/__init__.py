"""Decision telemetry and weather-correlation learning.

The parameter-learning layer that gave this package its name (parameter
optimizer, pattern analyzer, contextual controller, counterfactual
evaluator) was retired; see learning/telemetry.py for why. What remains is
the decision record set and the weather/load correlation model, which is
independent of it and still feeds the load forecast.
"""
