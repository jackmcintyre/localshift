{"body":"# Issue #170: Learning System — Detailed Implementation Plan\n\n## Executive Summary\n\n**Goal**: Implement a feedback loop that continuously optimizes the forecasting engine to minimize net electricity costs while avoiding the over-purchase/export-loss cycle.\n\n**Approach**: Adaptive Bayesian Optimization with Decision Quality Scoring — a pragmatic, incremental implementation that starts with measurement (observation-only) and builds toward automated parameter optimization.\n\n**Phases**: 5 phases, each building on the previous, designed for incremental rollout with zero risk to existing users.\n\n---\n\n## Table of Contents\n\n1. [Phase 1: Decision Outcome Tracking](#phase-1-decision-outcome-tracking)\n2. [Phase 2: Adaptive Parameter System](#phase-2-adaptive-parameter-system)\n3. [Phase 3: Pattern Recognition & Bias Detection](#phase-3-pattern-recognition--bias-detection)\n4. [Phase 4: Multi-Objective Optimization Controller](#phase-4-multi-objective-optimization-controller)\n5. [Phase 5: Monitoring & Observability](#phase-5-monitoring--observability)\n6. [Testing Strategy](#testing-strategy)\n7. [Documentation Updates](#documentation-updates)\n8. [Entity Count Impact](#entity-count-impact)\n9. [Rollout & Migration](#rollout--migration)\n\n---\n\n## Phase 1: Decision Outcome Tracking\n\n**Objective**: Measure the financial and strategic impact of every mode decision to create a ground-truth dataset for optimization. This phase is **observation-only** — no behavioral changes.\n\n### 1.1 New File: `computation_engine_lib/decision_outcome_tracker.py`\n\n**Purpose**: Record every mode transition with full context, then backfill actual outcomes after the decision period elapses.\n\n```python\nclass DecisionRecord:\n    \"\"\"Immutable record of a single mode decision and its outcome.\"\"\"\n    # Context at decision time\n    timestamp: datetime\n    mode_chosen: BatteryMode\n    previous_mode: BatteryMode\n    soc_at_decision: float\n    general_price_at_decision: float\n    feed_in_price_at_decision: float\n    forecast_solar_remaining_kwh: float\n    forecast_consumption_remaining_kwh: float\n    cheap_price_threshold: float\n    battery_target_soc: float\n    weather_condition: str  # from weather entity\n    day_of_week: int\n    hour_of_day: int\n    is_demand_window: bool\n    \n    # Outcome (backfilled after decision period ends)\n    actual_cost_during_period: float | None = None\n    actual_soc_change: float | None = None\n    actual_export_kwh: float | None = None\n    actual_import_kwh: float | None = None\n    duration_minutes: float | None = None\n    next_mode: BatteryMode | None = None\n    outcome_score: float | None = None  # computed quality score\n\n\nclass DecisionOutcomeTracker:\n    \"\"\"Tracks mode decisions and backfills outcomes.\"\"\"\n    \n    def __init__(self, hass: HomeAssistant, entry_id: str):\n        self._store = Store(hass, version=1, key=f\"{DOMAIN}.decision_outcomes.{entry_id}\")\n        self._pending_decisions: list[DecisionRecord] = []\n        self._completed_decisions: deque[DecisionRecord] = deque(maxlen=500)\n    \n    def record_decision(self, data: CoordinatorData, mode: BatteryMode, prev_mode: BatteryMode) -> None:\n        \"\"\"Called by StateMachine on every mode transition.\"\"\"\n    \n    def backfill_outcomes(self, data: CoordinatorData) -> None:\n        \"\"\"Called every periodic tick to fill in outcomes for completed decision periods.\"\"\"\n        # A decision period ends when the mode changes or after 30 minutes max\n    \n    def compute_outcome_score(self, record: DecisionRecord) -> float:\n        \"\"\"Score a decision outcome: lower net cost = better score.\n        \n        Components:\n        - cost_score: actual grid cost vs counterfactual (what would have happened in SELF_CONSUMPTION)\n        - export_penalty: penalize unnecessary exports (bought grid power then exported)\n        - target_score: bonus for reaching/maintaining SOC target\n        - cycling_penalty: penalize rapid mode changes\n        \"\"\"\n    \n    async def async_save(self) -> None:\n        \"\"\"Persist to HA storage.\"\"\"\n    \n    async def async_load(self) -> None:\n        \"\"\"Restore from HA storage on startup.\"\"\"\n    \n    def get_recent_decisions(self, hours: int = 24) -> list[DecisionRecord]:\n        \"\"\"Return decisions within the time window.\"\"\"\n    \n    def get_daily_summary(self) -> dict:\n        \"\"\"Aggregate today's decision outcomes into summary metrics.\"\"\"\n```\n\n**Integration points**:\n- `StateMachine.evaluate_state_machine()` → calls `tracker.record_decision()` on mode transitions\n- `Coordinator._handle_periodic_tick()` → calls `tracker.backfill_outcomes()`\n- `Coordinator._handle_midnight_reset()` → calls `tracker.async_save()` and archives daily summary\n\n### 1.2 New Dataclass: `PerformanceMetrics` in `coordinator_data.py`\n\n```python\n@dataclass\nclass PerformanceMetrics:\n    \"\"\"Aggregated performance metrics for the learning system.\"\"\"\n    # Daily metrics\n    total_decisions_today: int = 0\n    avg_decision_score_today: float = 0.0\n    grid_charge_efficiency: float = 0.0  # kWh used vs kWh needed\n    export_loss_ratio: float = 0.0       # exported kWh that was grid-purchased / total grid purchased\n    unnecessary_grid_charge_kwh: float = 0.0\n    \n    # Rolling metrics (7-day)\n    avg_decision_score_7d: float = 0.0\n    avg_daily_cost_7d: float = 0.0\n    cost_trend: str = \"stable\"  # \"improving\", \"stable\", \"degrading\"\n    \n    # Per-mode metrics\n    mode_durations_today: dict[str, float] = field(default_factory=dict)  # mode -> minutes\n    mode_cost_attribution: dict[str, float] = field(default_factory=dict)  # mode -> $ cost/saving\n```\n\n### 1.3 Modify `coordinator_data.py`: Add Fields\n\nAdd to `CoordinatorData`:\n```python\n    # --- Learning system (Issue #170 Phase 1) ---\n    performance_metrics: PerformanceMetrics = field(default_factory=PerformanceMetrics)\n    recent_decision_log: list[dict] = field(default_factory=list)  # last 24h of decisions for sensor\n    learning_status: str = \"observing\"  # \"observing\", \"tuning\", \"optimizing\"\n```\n\n### 1.4 Modify `coordinator.py`: Wire Tracker\n\n```python\n# In __init__:\nself.decision_tracker = DecisionOutcomeTracker(hass, entry.entry_id)\n\n# In async_setup (after other async_load calls):\nawait self.decision_tracker.async_load()\n\n# In _handle_periodic_tick:\nself.decision_tracker.backfill_outcomes(self.data)\nself.data.performance_metrics = self.decision_tracker.get_daily_summary()\n\n# In _handle_midnight_reset:\nawait self.decision_tracker.async_save()\n```\n\n### 1.5 Modify `state_machine.py`: Record Decisions\n\nIn `evaluate_state_machine()`, after a mode transition is confirmed:\n```python\nif new_mode != self._commanded_mode:\n    self.decision_tracker.record_decision(data, new_mode, self._commanded_mode)\n```\n\nThe `StateMachine.__init__` needs to accept a `decision_tracker` parameter, passed from the coordinator.\n\n### 1.6 Files Changed (Phase 1)\n\n| File | Change Type | Description |\n|------|-------------|-------------|\n| `computation_engine_lib/decision_outcome_tracker.py` | **NEW** | DecisionRecord, DecisionOutcomeTracker |\n| `coordinator_data.py` | MODIFY | Add PerformanceMetrics, learning fields |\n| `coordinator.py` | MODIFY | Wire tracker init, load, tick, save |\n| `state_machine.py` | MODIFY | Accept tracker, call record_decision |\n| `computation_engine_lib/__init__.py` | MODIFY | Export new class |\n\n## Phase 2: Adaptive Parameter System\n\n**Objective**: Create tunable parameters that are adjusted based on measured outcomes from Phase 1. This is the core optimization engine.\n\n### 2.1 Modify `const.py`: Define Optimizable Parameters\n\nAdd a structured definition of all parameters the learning system can tune:\n\n```python\n# --- Learning System: Optimizable Parameters (Issue #170 Phase 2) ---\n\n@dataclass(frozen=True)\nclass OptimizableParam:\n    \"\"\"Definition of a parameter that the learning system can adjust.\"\"\"\n    name: str\n    default: float\n    min_val: float\n    max_val: float\n    step: float\n    description: str\n\nOPTIMIZABLE_PARAMS: dict[str, OptimizableParam] = {\n    \"cheap_price_bias\": OptimizableParam(\n        name=\"cheap_price_bias\",\n        default=0.0,\n        min_val=-5.0,\n        max_val=5.0,\n        step=0.5,\n        description=\"Additive bias (c/kWh) to effective cheap price threshold. \"\n                    \"Positive = more willing to grid charge, Negative = more conservative.\",\n    ),\n    \"solar_confidence_factor\": OptimizableParam(\n        name=\"solar_confidence_factor\",\n        default=1.0,\n        min_val=0.5,\n        max_val=1.5,\n        step=0.05,\n        description=\"Multiplier on Solcast solar forecast. <1.0 = pessimistic (charge more), \"\n                    \">1.0 = optimistic (trust solar, charge less).\",\n    ),\n    \"overnight_drain_safety_margin\": OptimizableParam(\n        name=\"overnight_drain_safety_margin\",\n        default=0.0,\n        min_val=-5.0,\n        max_val=10.0,\n        step=1.0,\n        description=\"Extra SOC% safety margin added to overnight drain simulations. \"\n                    \"Positive = keep more buffer.\",\n    ),\n    \"grid_charge_soc_headroom\": OptimizableParam(\n        name=\"grid_charge_soc_headroom\",\n        default=0.0,\n        min_val=-5.0,\n        max_val=10.0,\n        step=1.0,\n        description=\"SOC% headroom above target. Positive = charge slightly above target \"\n                    \"to account for forecast errors.\",\n    ),\n    \"export_threshold_adjustment\": OptimizableParam(\n        name=\"export_threshold_adjustment\",\n        default=0.0,\n        min_val=-3.0,\n        max_val=3.0,\n        step=0.5,\n        description=\"Adjustment to proactive export profitability threshold (c/kWh). \"\n                    \"Positive = more conservative about exporting.\",\n    ),\n    \"consumption_forecast_bias\": OptimizableParam(\n        name=\"consumption_forecast_bias\",\n        default=0.0,\n        min_val=-0.5,\n        max_val=0.5,\n        step=0.05,\n        description=\"Additive bias (kW) to consumption forecast. \"\n                    \"Positive = assume higher consumption.\",\n    ),\n}\n```\n\n### 2.2 New Dataclass: `AdaptiveParameters` in `coordinator_data.py`\n\n```python\n@dataclass\nclass AdaptiveParameters:\n    \"\"\"Current state of learning system parameters.\"\"\"\n    values: dict[str, float] = field(default_factory=dict)  # param_name -> current_value\n    confidence: dict[str, float] = field(default_factory=dict)  # param_name -> 0.0-1.0\n    last_updated: datetime | None = None\n    update_count: int = 0\n    \n    def get(self, param_name: str, default: float = 0.0) -> float:\n        \"\"\"Get current parameter value, falling back to default.\"\"\"\n        return self.values.get(param_name, default)\n```\n\nAdd to `CoordinatorData`:\n```python\n    adaptive_params: AdaptiveParameters = field(default_factory=AdaptiveParameters)\n```\n\n### 2.3 Modify `grid_charge_decision.py`: Accept Adaptive Parameters\n\nCurrent `_should_grid_charge_at_slot` uses hardcoded thresholds. Modify to accept and apply adaptive parameters:\n\n```python\nclass GridChargeDecisionEngine:\n    def __init__(self, ..., adaptive_params: AdaptiveParameters | None = None):\n        self._adaptive_params = adaptive_params or AdaptiveParameters()\n    \n    def _calculate_local_effective_cheap_price(self, ...):\n        # EXISTING: base_cheap_price calculation\n        # NEW: Apply cheap_price_bias\n        bias = self._adaptive_params.get(\"cheap_price_bias\", 0.0)\n        adjusted_price = base_cheap_price + bias\n        return adjusted_price\n    \n    def _should_grid_charge_at_slot(self, ...):\n        # EXISTING: solar simulation\n        # NEW: Apply solar_confidence_factor to solar forecast values\n        solar_factor = self._adaptive_params.get(\"solar_confidence_factor\", 1.0)\n        adjusted_solar = raw_solar_forecast * solar_factor\n        \n        # EXISTING: overnight drain simulation\n        # NEW: Apply overnight_drain_safety_margin\n        drain_margin = self._adaptive_params.get(\"overnight_drain_safety_margin\", 0.0)\n        adjusted_soc_at_solar = simulated_soc - drain_margin\n        \n        # EXISTING: target SOC comparison\n        # NEW: Apply grid_charge_soc_headroom\n        headroom = self._adaptive_params.get(\"grid_charge_soc_headroom\", 0.0)\n        effective_target = battery_target + headroom\n```\n\n**Key principle**: All adjustments are additive/multiplicative offsets from the user's configured values. The user's settings remain the baseline; the learning system only applies small corrections.\n\n### 2.4 Modify `proactive_export.py`: Accept Adaptive Parameters\n\n```python\nclass ProactiveExportEngine:\n    def __init__(self, ..., adaptive_params: AdaptiveParameters | None = None):\n        self._adaptive_params = adaptive_params or AdaptiveParameters()\n    \n    def _should_proactive_export_at_slot(self, ...):\n        # EXISTING: profitability check (FIT > cheap_price + spread)\n        # NEW: Apply export_threshold_adjustment\n        export_adj = self._adaptive_params.get(\"export_threshold_adjustment\", 0.0)\n        effective_spread = export_spread + export_adj\n```\n\n### 2.5 Modify `forecast_computer.py`: Pass Adaptive Parameters\n\nThe `ForecastComputer.__init__` constructs `GridChargeDecisionEngine` and `ProactiveExportEngine`. Modify to:\n1. Accept `adaptive_params` in constructor\n2. Pass to sub-engines\n3. Apply `consumption_forecast_bias` in `_estimate_hourly_consumption_kw()`\n\n### 2.6 New File: `computation_engine_lib/parameter_optimizer.py`\n\n**Purpose**: Adjust parameter values based on outcome data from Phase 1.\n\n```python\nclass ParameterOptimizer:\n    \"\"\"Bayesian-inspired parameter optimizer using Thompson sampling.\"\"\"\n    \n    def __init__(self, hass: HomeAssistant, entry_id: str):\n        self._store = Store(hass, version=1, key=f\"{DOMAIN}.param_optimizer.{entry_id}\")\n        self._param_history: dict[str, list[tuple[float, float]]] = {}  # param -> [(value, score)]\n        self._current_params: AdaptiveParameters = AdaptiveParameters()\n        self._min_observations: int = 50  # minimum decisions before optimizing\n        self._update_interval_hours: int = 24  # re-optimize daily\n    \n    def should_update(self, decision_count: int) -> bool:\n        \"\"\"Check if enough data has accumulated for an update.\"\"\"\n        return (\n            decision_count >= self._min_observations\n            and self._hours_since_last_update() >= self._update_interval_hours\n        )\n    \n    def optimize(self, decisions: list[DecisionRecord]) -> AdaptiveParameters:\n        \"\"\"Run optimization using recent decision outcomes.\n        \n        Algorithm (Thompson Sampling variant):\n        1. For each parameter, group decisions by parameter value range\n        2. Compute mean outcome score for each range\n        3. Model each range as Beta distribution (success/failure based on score)\n        4. Sample from distributions, pick the range with highest sample\n        5. Set parameter to center of winning range\n        6. Add small exploration noise (Gaussian, decreasing over time)\n        \n        Constraints:\n        - Never adjust more than one step per update cycle\n        - Revert if 7-day rolling score decreases after adjustment\n        - Log all adjustments for observability\n        \"\"\"\n    \n    def _compute_parameter_score(\n        self, param_name: str, decisions: list[DecisionRecord]\n    ) -> dict[str, float]:\n        \"\"\"Score parameter influence by correlating param value with outcome quality.\"\"\"\n    \n    def get_current_params(self) -> AdaptiveParameters:\n        \"\"\"Return current optimized parameters.\"\"\"\n        return self._current_params\n    \n    async def async_save(self) -> None:\n        \"\"\"Persist optimizer state.\"\"\"\n    \n    async def async_load(self) -> None:\n        \"\"\"Restore optimizer state.\"\"\"\n```\n\n**Optimization safety rails**:\n- **Warm-up period**: No adjustments until 50+ decision records collected (~2-3 days)\n- **Step limits**: Parameters can only move one `step` per daily update\n- **Rollback**: If 7-day rolling score decreases for 3 consecutive days after an adjustment, revert to previous value\n- **Bounds**: Hard min/max from `OptimizableParam` definitions\n\n### 2.7 Modify `coordinator.py`: Wire Optimizer\n\n```python\n# In __init__:\nself.param_optimizer = ParameterOptimizer(hass, entry.entry_id)\n\n# In async_setup:\nawait self.param_optimizer.async_load()\n\n# In _handle_periodic_tick (after backfill_outcomes):\nif self.param_optimizer.should_update(len(self.decision_tracker._completed_decisions)):\n    self.data.adaptive_params = self.param_optimizer.optimize(\n        self.decision_tracker.get_recent_decisions(hours=168)  # 7 days\n    )\n\n# In _handle_midnight_reset:\nawait self.param_optimizer.async_save()\n```\n\n### 2.8 Modify `computation_engine.py`: Pass Params Through\n\nIn `compute_derived_values()`, pass `self.data.adaptive_params` to `ForecastComputer` when computing forecasts.\n\n### 2.9 Files Changed (Phase 2)\n\n| File | Change Type | Description |\n|------|-------------|-------------|\n| `const.py` | MODIFY | Add OptimizableParam, OPTIMIZABLE_PARAMS |\n| `coordinator_data.py` | MODIFY | Add AdaptiveParameters, adaptive_params field |\n| `computation_engine_lib/parameter_optimizer.py` | **NEW** | ParameterOptimizer, Thompson sampling |\n| `computation_engine_lib/grid_charge_decision.py` | MODIFY | Accept & apply adaptive params |\n| `computation_engine_lib/proactive_export.py` | MODIFY | Accept & apply adaptive params |\n| `computation_engine_lib/forecast_computer.py` | MODIFY | Pass adaptive params to sub-engines |\n| `computation_engine.py` | MODIFY | Pass adaptive params to forecast computer |\n| `coordinator.py` | MODIFY | Wire optimizer, trigger updates |\n\n## Phase 3: Pattern Recognition & Bias Detection\n\n**Objective**: Identify systematic issues (e.g., consistently over-buying on cloudy Mondays, under-charging before weekends) and feed bias corrections into Phase 2 parameters.\n\n### 3.1 New File: `computation_engine_lib/pattern_analyzer.py`\n\n**Purpose**: Aggregate decision outcome data by contextual dimensions to detect repeating patterns.\n\n```python\nclass PatternAnalyzer:\n    \"\"\"Detect systematic biases in decision quality across contextual dimensions.\"\"\"\n    \n    def __init__(self, hass: HomeAssistant, entry_id: str):\n        self._store = Store(hass, version=1, key=f\"{DOMAIN}.pattern_analysis.{entry_id}\")\n        self._pattern_data: dict[str, list[PatternBucket]] = {}\n    \n    def analyze(self, decisions: list[DecisionRecord]) -> PatternReport:\n        \"\"\"Run full pattern analysis across all dimensions.\n        \n        Dimensions analyzed:\n        1. Day of week (Mon-Sun)\n        2. Hour of day (0-23)\n        3. Weather condition (sunny, cloudy, rainy, etc.)\n        4. Season (summer, autumn, winter, spring)\n        5. Price regime (low, medium, high — based on daily avg price)\n        6. Solar availability (high/medium/low based on Solcast forecast)\n        \"\"\"\n    \n    def _analyze_dimension(\n        self, decisions: list[DecisionRecord], dimension: str, \n        key_func: Callable[[DecisionRecord], str]\n    ) -> dict[str, DimensionStats]:\n        \"\"\"Group decisions by dimension key, compute stats per group.\n        \n        For each group:\n        - mean_score, std_score\n        - over_charge_rate: % of decisions where grid charge was unnecessary\n        - under_charge_rate: % of decisions where SOC dropped below target\n        - export_loss_rate: % of decisions that resulted in export of grid-purchased energy\n        \"\"\"\n    \n    def detect_biases(self, report: PatternReport) -> list[BiasCorrection]:\n        \"\"\"Identify actionable biases from pattern report.\n        \n        A bias is detected when:\n        - A dimension group's mean score is >1 std dev below global mean\n        - The group has at least 10 samples (statistical significance)\n        - The bias is consistent across 2+ weeks\n        \n        Returns BiasCorrection objects that map to adaptive parameter adjustments.\n        Example: \"Cloudy days have 40% over-charge rate\" → \n            BiasCorrection(\n                condition=\"weather=cloudy\",\n                param=\"solar_confidence_factor\", \n                adjustment=+0.1,  # trust solar more on cloudy days (less grid charging)\n                confidence=0.7\n            )\n        \"\"\"\n    \n    async def async_save(self) -> None:\n        \"\"\"Persist pattern data.\"\"\"\n    \n    async def async_load(self) -> None:\n        \"\"\"Restore pattern data.\"\"\"\n\n\n@dataclass\nclass PatternBucket:\n    \"\"\"Aggregated stats for a dimension group.\"\"\"\n    key: str                    # e.g., \"monday\", \"cloudy\", \"summer\"\n    dimension: str              # e.g., \"day_of_week\", \"weather\", \"season\"\n    sample_count: int = 0\n    mean_score: float = 0.0\n    std_score: float = 0.0\n    over_charge_rate: float = 0.0\n    under_charge_rate: float = 0.0\n    export_loss_rate: float = 0.0\n\n\n@dataclass\nclass DimensionStats:\n    \"\"\"Stats for all groups within a dimension.\"\"\"\n    dimension: str\n    groups: dict[str, PatternBucket] = field(default_factory=dict)\n    global_mean: float = 0.0\n    global_std: float = 0.0\n\n\n@dataclass\nclass PatternReport:\n    \"\"\"Full pattern analysis report.\"\"\"\n    generated_at: datetime = field(default_factory=dt_util.now)\n    dimensions: dict[str, DimensionStats] = field(default_factory=dict)\n    biases_detected: list[BiasCorrection] = field(default_factory=list)\n    data_points_analyzed: int = 0\n\n\n@dataclass\nclass BiasCorrection:\n    \"\"\"A detected bias with recommended parameter adjustment.\"\"\"\n    condition: str          # human-readable condition description\n    dimension: str          # which dimension this bias was found in\n    group_key: str          # specific group (e.g., \"monday\", \"cloudy\")\n    param_name: str         # which optimizable param to adjust\n    adjustment: float       # recommended adjustment value\n    confidence: float       # 0.0-1.0 confidence in this correction\n    sample_count: int = 0   # how many decisions support this\n    weeks_observed: int = 0 # how many weeks this pattern has persisted\n```\n\n### 3.2 Modify `weather_correlation.py`: Enhanced Accuracy Tracking\n\nCurrent `WeatherCorrelationEngine` predicts consumption based on temperature. Enhance with historical accuracy tracking:\n\n```python\n# Add to WeatherCorrelationEngine:\ndef record_prediction_accuracy(\n    self, predicted_consumption_kw: float, actual_consumption_kw: float,\n    weather_condition: str, temperature: float\n) -> None:\n    \"\"\"Track prediction accuracy by weather condition.\n    \n    Stores (predicted, actual) pairs bucketed by:\n    - Weather condition (sunny, cloudy, rainy, etc.)\n    - Temperature range (cold <10°C, mild 10-25°C, hot >25°C)\n    \n    Used by PatternAnalyzer to detect weather-correlated forecasting biases.\n    \"\"\"\n\ndef get_accuracy_by_weather(self) -> dict[str, dict]:\n    \"\"\"Return prediction accuracy stats grouped by weather condition.\n    \n    Returns: {\n        \"sunny\": {\"mean_error\": -0.3, \"std_error\": 0.5, \"samples\": 42},\n        \"cloudy\": {\"mean_error\": +0.8, \"std_error\": 1.2, \"samples\": 28},\n        ...\n    }\n    \"\"\"\n```\n\n### 3.3 Modify `parameter_optimizer.py`: Accept Bias Corrections\n\nExtend the optimizer to incorporate pattern-based bias corrections:\n\n```python\nclass ParameterOptimizer:\n    def optimize(\n        self, decisions: list[DecisionRecord], \n        bias_corrections: list[BiasCorrection] | None = None\n    ) -> AdaptiveParameters:\n        \"\"\"Enhanced optimization that considers pattern biases.\n        \n        If bias_corrections are provided:\n        1. Apply high-confidence corrections (>0.8) directly as offsets\n        2. Use medium-confidence corrections (0.5-0.8) as priors in sampling\n        3. Ignore low-confidence corrections (<0.5)\n        \"\"\"\n```\n\n### 3.4 Modify `coordinator.py`: Wire Pattern Analyzer\n\n```python\n# In __init__:\nself.pattern_analyzer = PatternAnalyzer(hass, entry.entry_id)\n\n# In async_setup:\nawait self.pattern_analyzer.async_load()\n\n# Run pattern analysis weekly (or daily after sufficient data):\n# In _handle_midnight_reset:\nif self._days_since_last_pattern_analysis >= 7:\n    report = self.pattern_analyzer.analyze(\n        self.decision_tracker.get_recent_decisions(hours=168 * 4)  # 4 weeks\n    )\n    biases = self.pattern_analyzer.detect_biases(report)\n    self.data.adaptive_params = self.param_optimizer.optimize(\n        self.decision_tracker.get_recent_decisions(hours=168),\n        bias_corrections=biases\n    )\n    await self.pattern_analyzer.async_save()\n```\n\n### 3.5 Add to `coordinator_data.py`\n\n```python\n    # --- Learning system (Issue #170 Phase 3) ---\n    pattern_report_summary: dict = field(default_factory=dict)  # latest pattern analysis summary\n    active_bias_corrections: list[dict] = field(default_factory=list)  # currently active corrections\n```\n\n### 3.6 Files Changed (Phase 3)\n\n| File | Change Type | Description |\n|------|-------------|-------------|\n| `computation_engine_lib/pattern_analyzer.py` | **NEW** | PatternAnalyzer, BiasCorrection, PatternReport |\n| `weather_correlation.py` | MODIFY | Add accuracy tracking methods |\n| `computation_engine_lib/parameter_optimizer.py` | MODIFY | Accept bias corrections |\n| `coordinator.py` | MODIFY | Wire pattern analyzer, weekly analysis |\n| `coordinator_data.py` | MODIFY | Add pattern report fields |\n\n## Phase 4: Multi-Objective Optimization Controller\n\n**Objective**: Balance competing goals — minimize cost, avoid unnecessary exports, hit SOC targets, reduce battery cycling — using weighted scoring that adapts over time.\n\n### 4.1 New File: `computation_engine_lib/optimization_controller.py`\n\n**Purpose**: A high-level controller that sits above individual decision engines, providing real-time parameter adjustments based on current conditions and learned weights.\n\n```python\n@dataclass\nclass ObjectiveWeights:\n    \"\"\"Weights for competing optimization objectives.\"\"\"\n    cost_minimization: float = 0.50   # primary: minimize net electricity cost\n    export_avoidance: float = 0.20    # avoid exporting grid-purchased energy\n    target_achievement: float = 0.20  # reach SOC target by demand window\n    cycle_reduction: float = 0.10     # minimize battery charge/discharge cycles\n    \n    def normalize(self) -> \"ObjectiveWeights\":\n        \"\"\"Ensure weights sum to 1.0.\"\"\"\n        total = (self.cost_minimization + self.export_avoidance \n                 + self.target_achievement + self.cycle_reduction)\n        if total > 0:\n            return ObjectiveWeights(\n                cost_minimization=self.cost_minimization / total,\n                export_avoidance=self.export_avoidance / total,\n                target_achievement=self.target_achievement / total,\n                cycle_reduction=self.cycle_reduction / total,\n            )\n        return self\n\n\nclass OptimizationController:\n    \"\"\"Multi-objective controller that adjusts decision parameters in real-time.\"\"\"\n    \n    def __init__(\n        self, \n        hass: HomeAssistant, \n        entry_id: str,\n        decision_tracker: DecisionOutcomeTracker,\n        param_optimizer: ParameterOptimizer,\n        pattern_analyzer: PatternAnalyzer,\n    ):\n        self._store = Store(hass, version=1, key=f\"{DOMAIN}.opt_controller.{entry_id}\")\n        self._tracker = decision_tracker\n        self._optimizer = param_optimizer\n        self._analyzer = pattern_analyzer\n        self._weights = ObjectiveWeights()\n        self._weight_history: list[tuple[datetime, ObjectiveWeights]] = []\n    \n    def evaluate(self, data: CoordinatorData) -> AdaptiveParameters:\n        \"\"\"Real-time parameter evaluation considering current context.\n        \n        Called every computation cycle (every periodic tick).\n        \n        Steps:\n        1. Start with base adaptive params from ParameterOptimizer\n        2. Apply contextual adjustments based on current conditions:\n           - If SOC is low and approaching demand window → increase grid_charge_soc_headroom\n           - If today's export_loss_ratio is high → increase export_threshold_adjustment  \n           - If today's decision quality is low → reduce parameter exploration\n        3. Apply active bias corrections from PatternAnalyzer for current context\n           (e.g., if it's a cloudy Monday, apply the cloudy+Monday corrections)\n        4. Clamp all parameters to bounds\n        5. Return final AdaptiveParameters\n        \"\"\"\n    \n    def _apply_contextual_adjustments(\n        self, params: AdaptiveParameters, data: CoordinatorData\n    ) -> AdaptiveParameters:\n        \"\"\"Adjust parameters based on current real-time conditions.\n        \n        Contextual rules (heuristic layer on top of learned params):\n        \n        1. SOC Emergency: If SOC < 15% and before demand window\n           → temporarily boost cheap_price_bias by +3.0 (charge more aggressively)\n        \n        2. Export Leak Detection: If export_loss_ratio > 0.3 today\n           → boost export_threshold_adjustment by +1.0\n        \n        3. Forecast Confidence: If forecast_accuracy score < 0.5\n           → reduce solar_confidence_factor toward 0.8 (pessimistic)\n           → increase overnight_drain_safety_margin by +3.0\n        \n        4. Price Volatility: If price std dev > 2x normal\n           → reduce parameter exploration (tighten bounds)\n        \"\"\"\n    \n    def _apply_active_bias_corrections(\n        self, params: AdaptiveParameters, data: CoordinatorData\n    ) -> AdaptiveParameters:\n        \"\"\"Apply relevant bias corrections for current context.\n        \n        Matches current conditions (day_of_week, weather, etc.) against\n        active BiasCorrection entries and applies matching adjustments.\n        \"\"\"\n    \n    def update_weights(self, decisions: list[DecisionRecord]) -> None:\n        \"\"\"Adjust objective weights based on historical performance.\n        \n        If export losses are consistently high → increase export_avoidance weight\n        If target is rarely hit → increase target_achievement weight\n        If costs aren't improving → increase cost_minimization weight\n        \n        Weight adjustments are small (max 0.05 per update) and logged.\n        \"\"\"\n    \n    def score_decision(\n        self, record: DecisionRecord, weights: ObjectiveWeights\n    ) -> float:\n        \"\"\"Multi-objective scoring of a decision outcome.\n        \n        score = (weights.cost_minimization * cost_score \n                + weights.export_avoidance * export_score\n                + weights.target_achievement * target_score\n                + weights.cycle_reduction * cycle_score)\n        \n        Each component is normalized to [0, 1]:\n        - cost_score: 1.0 if net cost was optimal, 0.0 if worst case\n        - export_score: 1.0 if no unnecessary exports, 0.0 if all grid charge exported\n        - target_score: 1.0 if SOC target reached, 0.0 if missed by >20%\n        - cycle_score: 1.0 if mode stable, 0.0 if rapid cycling detected\n        \"\"\"\n    \n    async def async_save(self) -> None:\n        \"\"\"Persist controller state including weights.\"\"\"\n    \n    async def async_load(self) -> None:\n        \"\"\"Restore controller state.\"\"\"\n```\n\n### 4.2 Modify `computation_engine.py`: Integrate Controller\n\n```python\nclass ComputationEngine:\n    def __init__(self, ...):\n        # EXISTING init\n        # NEW: Accept optimization controller (optional for backward compat)\n        self._optimization_controller: OptimizationController | None = None\n    \n    def set_optimization_controller(self, controller: OptimizationController) -> None:\n        \"\"\"Set the optimization controller (called by coordinator after init).\"\"\"\n        self._optimization_controller = controller\n    \n    def compute_derived_values(self, data: CoordinatorData, ...) -> CoordinatorData:\n        # EXISTING computation steps...\n        \n        # NEW: Before forecast computation, get optimized params\n        if self._optimization_controller:\n            data.adaptive_params = self._optimization_controller.evaluate(data)\n        \n        # Pass adaptive_params to forecast computer\n        # EXISTING forecast computation with new params...\n```\n\n### 4.3 Modify `coordinator.py`: Wire Controller\n\n```python\n# In __init__ (after creating tracker, optimizer, analyzer):\nself.optimization_controller = OptimizationController(\n    hass, entry.entry_id,\n    self.decision_tracker,\n    self.param_optimizer,\n    self.pattern_analyzer,\n)\n\n# In async_setup:\nawait self.optimization_controller.async_load()\nself.computation_engine.set_optimization_controller(self.optimization_controller)\n\n# In _handle_midnight_reset:\nself.optimization_controller.update_weights(\n    self.decision_tracker.get_recent_decisions(hours=168)\n)\nawait self.optimization_controller.async_save()\n```\n\n### 4.4 New Switch: Enable Learning System\n\nAdd to `const.py`:\n```python\nCONF_ENABLE_LEARNING = \"enable_learning\"\n```\n\nAdd to `switch.py` — a new `EnableLearningSwitchEntity` that allows users to enable/disable the learning system entirely. When disabled, `OptimizationController.evaluate()` returns default (zero-offset) parameters.\n\nThis switch provides a user-visible toggle and safety valve.\n\n### 4.5 Add to `coordinator_data.py`\n\n```python\n    # --- Learning system (Issue #170 Phase 4) ---\n    optimization_weights: dict[str, float] = field(default_factory=dict)  # objective -> weight\n    contextual_adjustments_active: list[str] = field(default_factory=list)  # descriptions of active adjustments\n```\n\n### 4.6 Files Changed (Phase 4)\n\n| File | Change Type | Description |\n|------|-------------|-------------|\n| `computation_engine_lib/optimization_controller.py` | **NEW** | OptimizationController, ObjectiveWeights |\n| `computation_engine.py` | MODIFY | Accept & use controller |\n| `coordinator.py` | MODIFY | Wire controller, daily weight updates |\n| `const.py` | MODIFY | Add CONF_ENABLE_LEARNING |\n| `switch.py` | MODIFY | Add EnableLearningSwitch |\n| `coordinator_data.py` | MODIFY | Add optimization weight fields |\n\n## Phase 5: Monitoring & Observability\n\n**Objective**: Make learning system decisions visible, debuggable, and actionable for users.\n\n### 5.1 New Sensors in `sensor.py`\n\n#### 5.1.1 `LearningStatusSensor`\n\n```python\nclass LearningStatusSensor(LocalShiftSensorBase):\n    \"\"\"Shows current learning system status and parameter values.\"\"\"\n    \n    _attr_name = \"Learning Status\"\n    _attr_unique_id_suffix = \"learning_status\"\n    _attr_icon = \"mdi:brain\"\n    \n    @property\n    def native_value(self) -> str:\n        \"\"\"Return current learning phase: 'observing', 'tuning', 'optimizing', 'disabled'.\"\"\"\n        return self.coordinator.data.learning_status\n    \n    @property\n    def extra_state_attributes(self) -> dict:\n        \"\"\"Return current adaptive parameter values with confidence scores.\"\"\"\n        params = self.coordinator.data.adaptive_params\n        return {\n            \"update_count\": params.update_count,\n            \"last_updated\": params.last_updated.isoformat() if params.last_updated else None,\n            \"parameters\": {\n                name: {\n                    \"value\": params.values.get(name, defn.default),\n                    \"confidence\": params.confidence.get(name, 0.0),\n                    \"default\": defn.default,\n                    \"range\": f\"[{defn.min_val}, {defn.max_val}]\",\n                }\n                for name, defn in OPTIMIZABLE_PARAMS.items()\n            },\n            \"optimization_weights\": self.coordinator.data.optimization_weights,\n            \"active_bias_corrections\": self.coordinator.data.active_bias_corrections,\n            \"contextual_adjustments\": self.coordinator.data.contextual_adjustments_active,\n        }\n```\n\n#### 5.1.2 `DecisionQualitySensor`\n\n```python\nclass DecisionQualitySensor(LocalShiftSensorBase):\n    \"\"\"Rolling decision quality score.\"\"\"\n    \n    _attr_name = \"Decision Quality Score\"\n    _attr_unique_id_suffix = \"decision_quality\"\n    _attr_icon = \"mdi:chart-line\"\n    _attr_native_unit_of_measurement = \"%\"\n    _attr_state_class = SensorStateClass.MEASUREMENT\n    \n    @property\n    def native_value(self) -> float:\n        \"\"\"Return today's average decision quality as percentage.\"\"\"\n        return round(self.coordinator.data.performance_metrics.avg_decision_score_today * 100, 1)\n    \n    @property\n    def extra_state_attributes(self) -> dict:\n        metrics = self.coordinator.data.performance_metrics\n        return {\n            \"total_decisions_today\": metrics.total_decisions_today,\n            \"avg_score_7d\": round(metrics.avg_decision_score_7d * 100, 1),\n            \"cost_trend\": metrics.cost_trend,\n            \"grid_charge_efficiency\": round(metrics.grid_charge_efficiency * 100, 1),\n            \"export_loss_ratio\": round(metrics.export_loss_ratio * 100, 1),\n            \"mode_durations_today\": metrics.mode_durations_today,\n            \"mode_cost_attribution\": metrics.mode_cost_attribution,\n        }\n```\n\n#### 5.1.3 `DecisionHistorySensor`\n\n```python\nclass DecisionHistorySensor(LocalShiftSensorBase):\n    \"\"\"Recent decision history with outcomes.\"\"\"\n    \n    _attr_name = \"Decision History\"\n    _attr_unique_id_suffix = \"decision_history\"\n    _attr_icon = \"mdi:history\"\n    \n    @property\n    def native_value(self) -> int:\n        \"\"\"Return count of decisions in last 24h.\"\"\"\n        return len(self.coordinator.data.recent_decision_log)\n    \n    @property\n    def extra_state_attributes(self) -> dict:\n        return {\n            \"decisions\": self.coordinator.data.recent_decision_log[-20:],  # last 20\n            \"pattern_report\": self.coordinator.data.pattern_report_summary,\n        }\n```\n\n### 5.2 Enhanced Diagnostics in `diagnostics.py`\n\nAdd a `_get_learning_system_status` helper:\n\n```python\ndef _get_learning_system_status(coordinator) -> dict:\n    \"\"\"Gather learning system diagnostics.\"\"\"\n    return {\n        \"phase\": coordinator.data.learning_status,\n        \"decision_tracker\": {\n            \"pending_decisions\": len(coordinator.decision_tracker._pending_decisions),\n            \"completed_decisions\": len(coordinator.decision_tracker._completed_decisions),\n            \"daily_summary\": coordinator.decision_tracker.get_daily_summary(),\n        },\n        \"adaptive_parameters\": {\n            name: {\n                \"value\": coordinator.data.adaptive_params.values.get(name),\n                \"confidence\": coordinator.data.adaptive_params.confidence.get(name),\n            }\n            for name in OPTIMIZABLE_PARAMS\n        },\n        \"parameter_optimizer\": {\n            \"update_count\": coordinator.data.adaptive_params.update_count,\n            \"last_updated\": str(coordinator.data.adaptive_params.last_updated),\n        },\n        \"pattern_analysis\": coordinator.data.pattern_report_summary,\n        \"active_bias_corrections\": coordinator.data.active_bias_corrections,\n        \"optimization_weights\": coordinator.data.optimization_weights,\n        \"performance_metrics\": {\n            \"avg_score_today\": coordinator.data.performance_metrics.avg_decision_score_today,\n            \"avg_score_7d\": coordinator.data.performance_metrics.avg_decision_score_7d,\n            \"cost_trend\": coordinator.data.performance_metrics.cost_trend,\n            \"grid_charge_efficiency\": coordinator.data.performance_metrics.grid_charge_efficiency,\n            \"export_loss_ratio\": coordinator.data.performance_metrics.export_loss_ratio,\n        },\n    }\n```\n\nWire this into `async_get_config_entry_diagnostics` under a `\"learning_system\"` key.\n\n### 5.3 New Button: Reset Learning Data\n\nAdd to `button.py`:\n\n```python\nclass ResetLearningDataButton(LocalShiftButtonBase):\n    \"\"\"Button to reset all learning system data and start fresh.\"\"\"\n    \n    _attr_name = \"Reset Learning Data\"\n    _attr_unique_id_suffix = \"reset_learning\"\n    _attr_icon = \"mdi:brain-off\"\n    \n    async def async_press(self) -> None:\n        \"\"\"Reset all learning data.\"\"\"\n        coordinator = self.coordinator\n        coordinator.decision_tracker._pending_decisions.clear()\n        coordinator.decision_tracker._completed_decisions.clear()\n        coordinator.param_optimizer._current_params = AdaptiveParameters()\n        coordinator.data.adaptive_params = AdaptiveParameters()\n        coordinator.data.learning_status = \"observing\"\n        await coordinator.decision_tracker.async_save()\n        await coordinator.param_optimizer.async_save()\n        await coordinator.pattern_analyzer.async_save()\n        await coordinator.optimization_controller.async_save()\n```\n\n### 5.4 Notification Integration\n\nExtend `notification_service.py` to include learning system events in daily summary:\n\n```python\n# In daily summary notification:\nlearning_summary = (\n    f\"🧠 Learning: {data.learning_status} | \"\n    f\"Quality: {data.performance_metrics.avg_decision_score_today:.0%} | \"\n    f\"Trend: {data.performance_metrics.cost_trend}\"\n)\n```\n\n### 5.5 Files Changed (Phase 5)\n\n| File | Change Type | Description |\n|------|-------------|-------------|\n| `sensor.py` | MODIFY | Add LearningStatusSensor, DecisionQualitySensor, DecisionHistorySensor |\n| `diagnostics.py` | MODIFY | Add learning system diagnostic section |\n| `button.py` | MODIFY | Add ResetLearningDataButton |\n| `notification_service.py` | MODIFY | Include learning stats in daily summary |\n| `strings.json` | MODIFY | Add translation strings for new entities |\n| `translations/en.json` | MODIFY | Add English translations for new entities |\n\n## Testing Strategy\n\n### New Test Files\n\n| Test File | Covers | Key Test Cases |\n|-----------|--------|----------------|\n| `tests/test_decision_outcome_tracker.py` | Phase 1 | Record decisions, backfill outcomes, score computation, persistence, daily summary |\n| `tests/test_parameter_optimizer.py` | Phase 2 | Warm-up period, step limits, rollback, bounds clamping, Thompson sampling convergence |\n| `tests/test_pattern_analyzer.py` | Phase 3 | Dimension analysis, bias detection thresholds, correction mapping, insufficient data handling |\n| `tests/test_optimization_controller.py` | Phase 4 | Contextual adjustments, weight normalization, real-time evaluation, multi-objective scoring |\n| `tests/test_learning_integration.py` | All | End-to-end feedback loop: decisions → outcomes → optimization → improved decisions |\n\n### Key Test Scenarios\n\n**Phase 1 Tests**:\n- Decision recorded with correct context snapshot\n- Backfill correctly computes cost/export/SOC changes\n- Outcome score penalizes grid-charge-then-export pattern\n- Persistence survives HA restart (save/load cycle)\n- Daily summary aggregation is correct\n- Edge case: rapid mode transitions (< 1 minute apart)\n\n**Phase 2 Tests**:\n- No optimization before warm-up period (50 decisions)\n- Parameter only moves one step per update\n- Rollback triggers after 3 days of declining score\n- All parameters stay within bounds\n- Default (zero-offset) params produce identical behavior to current system\n\n**Phase 3 Tests**:\n- Bias detected for consistently bad cloudy-day decisions\n- Corrections only generated with sufficient sample size (≥10)\n- High-confidence corrections applied, low-confidence ignored\n- No false positives on small/random data\n\n**Phase 4 Tests**:\n- SOC emergency contextual adjustment triggers correctly\n- Export leak detection responds to high export_loss_ratio\n- Weight normalization always sums to 1.0\n- Disabled learning switch returns zero-offset params\n\n**Integration Tests**:\n- Full cycle: 100 simulated decisions → optimizer adjusts params → next batch of decisions has higher scores\n- Learning system disabled mid-cycle doesn't crash\n- Reset button clears all state correctly\n\n### Existing Test Modifications\n\nTests that mock `GridChargeDecisionEngine` or `ProactiveExportEngine` will need to account for the new `adaptive_params` parameter. Strategy:\n- Default `adaptive_params=None` in constructors ensures backward compat\n- Existing tests continue to pass unchanged\n- New tests explicitly pass `AdaptiveParameters` with non-zero values to verify adjustment application\n\n---\n\n## Documentation Updates\n\n### Files to Update\n\n| Document | Updates Required |\n|----------|-----------------|\n| `docs/ARCHITECTURE.md` | Add \"Learning System\" section describing the feedback loop architecture, data flow diagram, and component interactions |\n| `docs/ENTITY_REFERENCE.md` | Add entries for 3 new sensors (LearningStatus, DecisionQuality, DecisionHistory), 1 new switch (Enable Learning), 1 new button (Reset Learning Data) |\n| `README.md` | Update entity count tables, add Learning System feature description, update sensor/switch/button counts |\n| `docs/DEVELOPER_GUIDE.md` | Add section on learning system internals, how to extend optimizable parameters, testing the feedback loop |\n| `docs/TROUBLESHOOTING.md` | Add FAQ entries: \"How do I reset learning data?\", \"Why is my learning status stuck on 'observing'?\", \"How long before the system starts optimizing?\" |\n| `.clinerules` | Update entity counts |\n\n### New Documentation\n\nConsider a new `docs/LEARNING_SYSTEM.md` dedicated document covering:\n- How the learning system works (user-friendly explanation)\n- What the decision quality score means\n- How to interpret adaptive parameter values\n- Expected timeline (Phase 1 observation → Phase 2 tuning → full optimization)\n- How to reset and start fresh\n\n---\n\n## Entity Count Impact\n\n### New Entities Added\n\n| Category | New Entities | Details |\n|----------|-------------|---------|\n| Sensors | +3 | LearningStatusSensor, DecisionQualitySensor, DecisionHistorySensor |\n| Switches | +1 | EnableLearningSwitch |\n| Buttons | +1 | ResetLearningDataButton |\n| **Total New** | **+5** | |\n\n### Updated Entity Counts\n\n| Category | Current | After Issue #170 |\n|----------|---------|-------------------|\n| Sensors | 18 | **21** |\n| Binary Sensors | 10 | 10 |\n| Switches | 10 | **11** |\n| Numbers | 8 | 8 |\n| Buttons | 5 | **6** |\n| **Total** | **51** | **56** |\n\n---\n\n## Rollout & Migration\n\n### Phase-by-Phase Rollout\n\n| Phase | PR Scope | Behavioral Impact | Risk |\n|-------|----------|-------------------|------|\n| Phase 1 | Foundation — tracking only | **None** — pure observation | Very Low |\n| Phase 2 | Parameter system + optimizer | **None until warm-up** — 50+ decisions before first adjustment | Low |\n| Phase 3 | Pattern analysis | **None** — feeds into Phase 2 optimizer | Low |\n| Phase 4 | Real-time controller | **Active** — applies contextual adjustments | Medium |\n| Phase 5 | Observability | **None** — sensors & diagnostics only | Very Low |\n\n### Recommended Implementation Order\n\n1. **Phase 1 + Phase 5 (partial)**: Ship decision tracking + learning status sensor together. Users can see the system observing immediately.\n2. **Phase 2**: Ship adaptive parameter system. The warm-up period ensures no impact until enough data collected.\n3. **Phase 3**: Ship pattern analyzer. Enhances Phase 2 but has no independent behavioral impact.\n4. **Phase 4**: Ship optimization controller + enable learning switch. This is the only phase with active behavioral impact, gated behind the switch.\n5. **Phase 5 (remainder)**: Ship remaining observability features.\n\n### Backward Compatibility\n\n- All new `CoordinatorData` fields have safe defaults (empty dicts, zero values, \"observing\" status)\n- All new constructor parameters are optional with `None` defaults\n- Existing tests pass without modification\n- Enable Learning switch defaults to **OFF** — users must explicitly opt in to active optimization\n- Learning system can be completely disabled at any time with no data loss\n\n### Persistence & Storage\n\nNew HA Store keys (all scoped to entry_id):\n- `localshift.decision_outcomes.{entry_id}` — decision history\n- `localshift.param_optimizer.{entry_id}` — optimizer state\n- `localshift.pattern_analysis.{entry_id}` — pattern data\n- `localshift.opt_controller.{entry_id}` — controller weights\n\nAll stores use HA's `Store` helper (same pattern as existing `cost_tracker.py`). Data survives HA restarts.\n\n### Estimated Implementation Effort\n\n| Phase | Estimated Lines of Code | Estimated Test Lines | Complexity |\n|-------|------------------------|---------------------|------------|\n| Phase 1 | ~300 | ~400 | Medium |\n| Phase 2 | ~400 | ~500 | High |\n| Phase 3 | ~350 | ~400 | Medium |\n| Phase 4 | ~300 | ~350 | High |\n| Phase 5 | ~200 | ~150 | Low |\n| **Total** | **~1,550** | **~1,800** | |\n\n---\n\n## Complete File Change Summary\n\n### New Files (4)\n\n| File | Phase | Description |\n|------|-------|-------------|\n| `computation_engine_lib/decision_outcome_tracker.py` | 1 | Decision recording & outcome scoring |\n| `computation_engine_lib/parameter_optimizer.py` | 2 | Bayesian parameter optimization |\n| `computation_engine_lib/pattern_analyzer.py` | 3 | Pattern recognition & bias detection |\n| `computation_engine_lib/optimization_controller.py` | 4 | Multi-objective real-time controller |\n\n### Modified Files (14)\n\n| File | Phases | Changes |\n|------|--------|---------|\n| `coordinator_data.py` | 1,2,3,4 | PerformanceMetrics, AdaptiveParameters, learning fields |\n| `coordinator.py` | 1,2,3,4 | Wire all learning components |\n| `state_machine.py` | 1 | Accept decision tracker, record transitions |\n| `const.py` | 2,4 | OptimizableParam, OPTIMIZABLE_PARAMS, CONF_ENABLE_LEARNING |\n| `computation_engine.py` | 2,4 | Pass adaptive params, accept controller |\n| `computation_engine_lib/grid_charge_decision.py` | 2 | Accept & apply adaptive params |\n| `computation_engine_lib/proactive_export.py` | 2 | Accept & apply adaptive params |\n| `computation_engine_lib/forecast_computer.py` | 2 | Pass adaptive params to sub-engines |\n| `computation_engine_lib/__init__.py` | 1 | Export new classes |\n| `weather_correlation.py` | 3 | Add accuracy tracking methods |\n| `switch.py` | 4 | Add EnableLearningSwitch |\n| `sensor.py` | 5 | Add 3 learning sensors |\n| `button.py` | 5 | Add ResetLearningDataButton |\n| `diagnostics.py` | 5 | Add learning system diagnostics |\n| `notification_service.py` | 5 | Include learning stats in daily summary |\n| `strings.json` | 5 | Translation strings for new entities |\n| `translations/en.json` | 5 | English translations for new entities |\n\n### New Test Files (5)\n\n| File | Description |\n|------|-------------|\n| `tests/test_decision_outcome_tracker.py` | Phase 1 tests |\n| `tests/test_parameter_optimizer.py` | Phase 2 tests |\n| `tests/test_pattern_analyzer.py` | Phase 3 tests |\n| `tests/test_optimization_controller.py` | Phase 4 tests |\n| `tests/test_learning_integration.py` | End-to-end integration tests |\n","title":"Learning system"}
