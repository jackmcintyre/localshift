# Ignore all files in docs/superpowers directory
# This directory contains AI-generated plans and specs that are session-specific
*

# But track subdirectories
!plans/
!specs/

# And track specific markdown files in subdirectories
!plans/*.md
!specs/*.md

# Ignore session artifacts
plans/*-session-*.md
specs/*-session-*.md
plans/*.tmp
specs/*.tmp
